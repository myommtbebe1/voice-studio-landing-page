# Base URL for this app (Pricing / Buy Points page). Used as Stripe cancel/return URL.
# Set to your project URL so the "back" button from checkout returns here, not another site.
# Local: http://localhost:4200
# Production: https://your-domain.com
VITE_APP_BASE_URL=http://localhost:4200
